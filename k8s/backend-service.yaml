---
apiVersion: v1
kind: Service
metadata:
  name: backend-service
spec:
  selector:
    app: relayiq-backend
  ports:
    - protocol: TCP
      port: 5000       # the port your backend container exposes
      targetPort: 5000
  type: ClusterIP
